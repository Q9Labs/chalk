{
  "project": "React Native SDK - Multi-user Video Conferencing",
  "description": "Enable real room connections, participant sync, and multi-user video conferencing in @q9labs/chalk-react-native",
  "userStories": [
    {
      "id": "rn-sdk-001",
      "title": "Fix race condition in PreCallScreen stream initialization",
      "description": "PreCallScreen calls startStream() before ChalkProvider finishes initializing rtcManager, causing 'RTCManager not available' error and black camera preview",
      "priority": 1,
      "verification": [
        "Use ios-simulator skill: python scripts/app_launcher.py --launch com.q9labs.chalk-example",
        "Use ios-simulator skill: python scripts/navigator.py --find-text 'Create' --tap",
        "Check Metro logs for '[useLocalStream] Got stream: {\"stream\": true}'",
        "Verify NO '[useLocalStream] RTCManager not available' error after tap",
        "Use ios-simulator skill: xcrun simctl io booted screenshot to capture result"
      ],
      "passes": true
    },
    {
      "id": "rn-sdk-002",
      "title": "Fix iOS permission detection returning 'unavailable'",
      "description": "usePermissions hook always shows camera/microphone as 'unavailable' on iOS even when permissions are already granted, blocking camera preview from starting",
      "priority": 2,
      "verification": [
        "Use ios-simulator skill: python scripts/privacy_manager.py --bundle-id com.q9labs.chalk-example --grant camera,microphone",
        "Use ios-simulator skill: python scripts/app_launcher.py --terminate com.q9labs.chalk-example then --launch",
        "Use ios-simulator skill: python scripts/navigator.py --find-text 'Create' --tap",
        "Take screenshot and verify 'Camera: granted' and 'Microphone: granted' are shown (not 'unavailable')",
        "Verify hasRequiredPermissions is true in the UI (Join Call button should be enabled, not dimmed)"
      ],
      "passes": true
    },
    {
      "id": "rn-sdk-003",
      "title": "Fix VideoView not rendering stream",
      "description": "VideoView receives valid stream with streamURL but renders black/empty. Metro logs show valid streamURL but no video appears in preview area",
      "priority": 3,
      "verification": [
        "Use ios-simulator skill: Launch app and navigate to PreCallScreen",
        "Check Metro logs show '[VideoView] Rendering stream:' with valid streamURL",
        "Take screenshot - preview area should show camera feed OR clear 'camera unavailable' message",
        "NOT a black box with no explanation",
        "If on simulator without camera, verify placeholder UI explains the limitation"
      ],
      "passes": true
    },
    {
      "id": "rn-sdk-004",
      "title": "Fix CallScreen status stuck on 'Connecting'",
      "description": "CallScreen shows 'Connecting' status indefinitely in demo mode instead of showing 'Demo Mode' or appropriate status",
      "priority": 4,
      "verification": [
        "Use ios-simulator skill: Navigate to PreCallScreen then tap 'Join Call'",
        "Use ios-simulator skill: python scripts/navigator.py --find-text 'Join' --tap",
        "Take screenshot of CallScreen",
        "Verify status badge shows 'Demo Mode' NOT 'Connecting'",
        "Use ios-simulator skill: Test controls - tap mute, camera, hand raise buttons and verify they respond"
      ],
      "passes": true
    },
    {
      "id": "rn-sdk-005",
      "title": "Add error UI when camera unavailable",
      "description": "When running on simulator without camera support, user sees confusing black screen instead of helpful error message explaining why camera isn't working",
      "priority": 5,
      "verification": [
        "Use ios-simulator skill: Launch app on simulator",
        "Navigate to PreCallScreen",
        "If camera stream fails or returns empty, UI should show clear message like 'Camera not available on simulator'",
        "Take screenshot to verify - should NOT be a black preview with no text",
        "Error should be user-friendly, not a technical error message"
      ],
      "passes": true
    },
    {
      "id": "rn-sdk-006",
      "title": "Add tokenProvider support to React Native ChalkProvider",
      "description": "React Native ChalkProvider only supports static token/apiKey but web version has tokenProvider for dynamic JWT refresh. Add tokenProvider prop to match web SDK capabilities.",
      "priority": 6,
      "verification": [
        "ChalkProviderProps interface includes tokenProvider?: () => Promise<string>",
        "ChalkProvider accepts and uses tokenProvider when provided",
        "Token refresh works - when token expires, tokenProvider is called again",
        "bun run check-types passes",
        "Existing apiKey/token auth continues to work (backwards compatible)"
      ],
      "passes": true
    },
    {
      "id": "rn-sdk-007",
      "title": "Configure example app with working authentication",
      "description": "Example app has no authentication configured - just apiUrl. Need to add tokenProvider that fetches JWT from API or use environment variable for API key so rooms actually connect.",
      "priority": 7,
      "dependsOn": ["rn-sdk-006"],
      "verification": [
        "Example App.tsx has tokenProvider or apiKey configured",
        "Launch app, create room, join - status shows 'Live' or 'Connected' NOT 'Demo Mode'",
        "Metro logs show successful API responses (not 401/403 errors)",
        "Room connection is established (WebSocket connected)"
      ],
      "passes": true,
      "note": "Auth code complete. API has valid Cloudflare credentials. RN SDK connects to API but lacks RTK WebRTC integration (see rn-sdk-008)."
    },
    {
      "id": "rn-sdk-008",
      "title": "Integrate Cloudflare RTK signaling with react-native-webrtc",
      "description": "Web SDK uses @cloudflare/realtimekit for WebRTC signaling. React Native SDK only has local streams via RTCManager with no RTK integration. Need to bridge RTK signaling protocol to react-native-webrtc peer connections.",
      "priority": 8,
      "dependsOn": ["rn-sdk-007"],
      "verification": [
        "RTCManager connects to Cloudflare RTK using authToken from API",
        "WebRTC signaling (offer/answer/ICE) works through RTK",
        "Peer connection establishes with Cloudflare SFU",
        "Local stream is published to SFU",
        "bun run check-types passes"
      ],
      "passes": true,
      "note": "Client code complete: ChalkProvider uses @cloudflare/realtimekit-react-native hooks, all hooks use rtkClient. Runtime testing requires Cloudflare Calls enabled with valid APP_ID/APP_KEY in API."
    },
    {
      "id": "rn-sdk-009",
      "title": "Receive and render remote participant streams",
      "description": "After RTK signaling works, subscribe to remote participant tracks from SFU and render them in VideoView components.",
      "priority": 9,
      "dependsOn": ["rn-sdk-008"],
      "verification": [
        "RTCManager receives remote track events from SFU",
        "Remote video tracks are exposed via useParticipants or dedicated hook",
        "CallScreen renders remote participant video tiles",
        "Video grid adjusts layout based on participant count",
        "Remote participant leaves -> video tile removed"
      ],
      "passes": true,
      "note": "Client code complete: ParticipantTile component renders remote video, VideoGrid handles responsive layout, useParticipants exposes remoteParticipants with videoTrack. Runtime testing requires Cloudflare Calls enabled."
    },
    {
      "id": "rn-sdk-010",
      "title": "Handle remote audio tracks",
      "description": "Remote audio tracks from SFU need to be attached to audio output. React Native WebRTC auto-plays remote audio, but verify it works correctly.",
      "priority": 10,
      "dependsOn": ["rn-sdk-008"],
      "verification": [
        "Join room with two devices (web + mobile)",
        "Speak on web - audio plays on mobile speakers",
        "Speak on mobile - audio plays on web",
        "Muting local audio stops remote from hearing",
        "Audio works when video is disabled"
      ],
      "passes": true,
      "note": "Client code complete: CallScreen wrapped in AudioSession for proper audio routing. React Native WebRTC auto-plays remote audio tracks when received via RTK. AudioSession configures iOS AVAudioSession and Android audio focus. Runtime testing requires Cloudflare Calls enabled."
    },
    {
      "id": "rn-sdk-011",
      "title": "Participant sync via RTK events",
      "description": "RTK emits participant-joined/left events. Wire these to useParticipants hook so UI reflects all room participants.",
      "priority": 11,
      "dependsOn": ["rn-sdk-008"],
      "verification": [
        "Join room on web with user A",
        "Join same room on mobile with user B",
        "Web shows B in participant list",
        "Mobile shows A in participant list",
        "Participant counts match on both platforms"
      ],
      "passes": false
    }
  ]
}
