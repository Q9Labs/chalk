# Whisper transcription worker dependencies

# Faster-whisper: CTranslate2-based Whisper implementation
# 4x faster than OpenAI whisper with same accuracy
faster-whisper==1.2.1

# Redis client for job queue
redis==5.0.1

# Axiom logging (structured wide events)
axiom-py==0.9.1

# OpenTelemetry tracing (direct OTLP/HTTP export to Axiom)
opentelemetry-api==1.37.0
opentelemetry-sdk==1.37.0
opentelemetry-exporter-otlp-proto-http==1.37.0

# CloudWatch metrics publishing
boto3==1.42.44

# HTTP client (required by faster-whisper for model downloads)
requests>=2.31.0

# CTranslate2 with CUDA support (installed by faster-whisper)
# ctranslate2>=4.0.0
