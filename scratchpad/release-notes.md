## Features

- **More expressive generated avatars** — meeting avatar fallbacks now keep FaceHash’s interactive feel while preserving the same local on/off preference and richer 3D styling.

## Bug Fixes

- **Pre-join controls feel tighter** — the lobby mic and camera controls now use the more compact floating treatment with clearer labels and inline device pickers.
- **Destructive actions read more clearly** — the leave-confirmation dialog now uses the same stronger red confirm button styling as the in-meeting leave control.

## Technical Notes

- Cut release `v0.0.74` on `2026-03-12`.
- Updated package versions for `@q9labs/chalk-core`, `@q9labs/chalk-react`, `@q9labs/chalk-ui`, and `@q9labs/chalk-whiteboard`.
- Added the `facehash` dependency to `@q9labs/chalk-react` and refreshed release metadata for the latest publish cycle.
