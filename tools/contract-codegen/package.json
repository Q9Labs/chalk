{
  "name": "@chalk/contract-codegen",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "proof": "node src/cli.mjs proof",
    "generate": "node src/cli.mjs generate",
    "check": "node src/cli.mjs check",
    "check-generated": "node src/cli.mjs check",
    "test": "pnpm exec vitest run",
    "lint": "pnpm exec oxfmt --check . && pnpm exec tsc --project tsconfig.json --noEmit",
    "check-types": "pnpm exec tsc --project tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@typespec/compiler": "1.13.0",
    "@typespec/http": "1.13.0",
    "effect": "4.0.0-beta.94"
  }
}
