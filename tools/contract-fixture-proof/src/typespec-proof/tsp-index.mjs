// @ts-check

import { $bodyLimit, $closeCode, $commandError, $opaqueJson, $operationId, $rateLimit, $syncCommand, $syncConnection, $syncEvent, $syncFrame, $syncHello, $syncProtocol, $unionDiscriminator, $wireError } from "./decorators.mjs";

export const $decorators = {
  "Chalk.Proof": {
    bodyLimit: $bodyLimit,
    closeCode: $closeCode,
    commandError: $commandError,
    opaqueJson: $opaqueJson,
    operationId: $operationId,
    rateLimit: $rateLimit,
    syncCommand: $syncCommand,
    syncConnection: $syncConnection,
    syncEvent: $syncEvent,
    syncFrame: $syncFrame,
    syncHello: $syncHello,
    syncProtocol: $syncProtocol,
    unionDiscriminator: $unionDiscriminator,
    wireError: $wireError,
  },
};
