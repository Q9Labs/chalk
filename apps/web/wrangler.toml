name = "chalk-web"
compatibility_date = "2024-12-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "apps/web/.output/public"

[build]
command = "bun install && bun run build --filter=web"

[vars]
VITE_API_URL = "https://chalk-api.q9labs.ai"
