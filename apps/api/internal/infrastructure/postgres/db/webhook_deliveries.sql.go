// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: webhook_deliveries.sql

package db

import (
	"context"

	"github.com/google/uuid"
	"github.com/jackc/pgx/v5/pgtype"
)

const createWebhookDelivery = `-- name: CreateWebhookDelivery :one

INSERT INTO webhook_deliveries (tenant_id, room_id, recording_id, transcript_id, event_type, webhook_url, payload, next_retry_at)
VALUES ($1, $2, $3, $4, $5, $6, $7, NOW())
RETURNING id, tenant_id, room_id, recording_id, transcript_id, event_type, webhook_url, payload, status, attempts, max_attempts, last_error, next_retry_at, delivered_at, created_at
`

type CreateWebhookDeliveryParams struct {
	TenantID     uuid.UUID   `db:"tenant_id" json:"tenant_id"`
	RoomID       uuid.UUID   `db:"room_id" json:"room_id"`
	RecordingID  pgtype.UUID `db:"recording_id" json:"recording_id"`
	TranscriptID pgtype.UUID `db:"transcript_id" json:"transcript_id"`
	EventType    string      `db:"event_type" json:"event_type"`
	WebhookUrl   string      `db:"webhook_url" json:"webhook_url"`
	Payload      []byte      `db:"payload" json:"payload"`
}

// Webhook Deliveries Queries
// CRUD operations for webhook delivery tracking
func (q *Queries) CreateWebhookDelivery(ctx context.Context, arg CreateWebhookDeliveryParams) (WebhookDelivery, error) {
	row := q.db.QueryRow(ctx, createWebhookDelivery,
		arg.TenantID,
		arg.RoomID,
		arg.RecordingID,
		arg.TranscriptID,
		arg.EventType,
		arg.WebhookUrl,
		arg.Payload,
	)
	var i WebhookDelivery
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.RoomID,
		&i.RecordingID,
		&i.TranscriptID,
		&i.EventType,
		&i.WebhookUrl,
		&i.Payload,
		&i.Status,
		&i.Attempts,
		&i.MaxAttempts,
		&i.LastError,
		&i.NextRetryAt,
		&i.DeliveredAt,
		&i.CreatedAt,
	)
	return i, err
}

const getFailedWebhookDeliveries = `-- name: GetFailedWebhookDeliveries :many
SELECT id, tenant_id, room_id, recording_id, transcript_id, event_type, webhook_url, payload, status, attempts, max_attempts, last_error, next_retry_at, delivered_at, created_at FROM webhook_deliveries
WHERE status = 'failed'
  AND attempts >= max_attempts
ORDER BY created_at DESC
LIMIT $1 OFFSET $2
`

type GetFailedWebhookDeliveriesParams struct {
	Limit  int32 `db:"limit" json:"limit"`
	Offset int32 `db:"offset" json:"offset"`
}

func (q *Queries) GetFailedWebhookDeliveries(ctx context.Context, arg GetFailedWebhookDeliveriesParams) ([]WebhookDelivery, error) {
	rows, err := q.db.Query(ctx, getFailedWebhookDeliveries, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []WebhookDelivery{}
	for rows.Next() {
		var i WebhookDelivery
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.RoomID,
			&i.RecordingID,
			&i.TranscriptID,
			&i.EventType,
			&i.WebhookUrl,
			&i.Payload,
			&i.Status,
			&i.Attempts,
			&i.MaxAttempts,
			&i.LastError,
			&i.NextRetryAt,
			&i.DeliveredAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getPendingWebhookDeliveries = `-- name: GetPendingWebhookDeliveries :many
SELECT id, tenant_id, room_id, recording_id, transcript_id, event_type, webhook_url, payload, status, attempts, max_attempts, last_error, next_retry_at, delivered_at, created_at FROM webhook_deliveries
WHERE status IN ('pending', 'failed')
  AND next_retry_at <= NOW()
  AND attempts < max_attempts
ORDER BY next_retry_at ASC
LIMIT $1
`

func (q *Queries) GetPendingWebhookDeliveries(ctx context.Context, limit int32) ([]WebhookDelivery, error) {
	rows, err := q.db.Query(ctx, getPendingWebhookDeliveries, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []WebhookDelivery{}
	for rows.Next() {
		var i WebhookDelivery
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.RoomID,
			&i.RecordingID,
			&i.TranscriptID,
			&i.EventType,
			&i.WebhookUrl,
			&i.Payload,
			&i.Status,
			&i.Attempts,
			&i.MaxAttempts,
			&i.LastError,
			&i.NextRetryAt,
			&i.DeliveredAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getWebhookDeliveriesByRoom = `-- name: GetWebhookDeliveriesByRoom :many
SELECT id, tenant_id, room_id, recording_id, transcript_id, event_type, webhook_url, payload, status, attempts, max_attempts, last_error, next_retry_at, delivered_at, created_at FROM webhook_deliveries
WHERE room_id = $1
ORDER BY created_at DESC
`

func (q *Queries) GetWebhookDeliveriesByRoom(ctx context.Context, roomID uuid.UUID) ([]WebhookDelivery, error) {
	rows, err := q.db.Query(ctx, getWebhookDeliveriesByRoom, roomID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []WebhookDelivery{}
	for rows.Next() {
		var i WebhookDelivery
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.RoomID,
			&i.RecordingID,
			&i.TranscriptID,
			&i.EventType,
			&i.WebhookUrl,
			&i.Payload,
			&i.Status,
			&i.Attempts,
			&i.MaxAttempts,
			&i.LastError,
			&i.NextRetryAt,
			&i.DeliveredAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getWebhookDeliveriesByTenant = `-- name: GetWebhookDeliveriesByTenant :many
SELECT id, tenant_id, room_id, recording_id, transcript_id, event_type, webhook_url, payload, status, attempts, max_attempts, last_error, next_retry_at, delivered_at, created_at FROM webhook_deliveries
WHERE tenant_id = $1
ORDER BY created_at DESC
LIMIT $2 OFFSET $3
`

type GetWebhookDeliveriesByTenantParams struct {
	TenantID uuid.UUID `db:"tenant_id" json:"tenant_id"`
	Limit    int32     `db:"limit" json:"limit"`
	Offset   int32     `db:"offset" json:"offset"`
}

func (q *Queries) GetWebhookDeliveriesByTenant(ctx context.Context, arg GetWebhookDeliveriesByTenantParams) ([]WebhookDelivery, error) {
	rows, err := q.db.Query(ctx, getWebhookDeliveriesByTenant, arg.TenantID, arg.Limit, arg.Offset)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []WebhookDelivery{}
	for rows.Next() {
		var i WebhookDelivery
		if err := rows.Scan(
			&i.ID,
			&i.TenantID,
			&i.RoomID,
			&i.RecordingID,
			&i.TranscriptID,
			&i.EventType,
			&i.WebhookUrl,
			&i.Payload,
			&i.Status,
			&i.Attempts,
			&i.MaxAttempts,
			&i.LastError,
			&i.NextRetryAt,
			&i.DeliveredAt,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const getWebhookDelivery = `-- name: GetWebhookDelivery :one
SELECT id, tenant_id, room_id, recording_id, transcript_id, event_type, webhook_url, payload, status, attempts, max_attempts, last_error, next_retry_at, delivered_at, created_at FROM webhook_deliveries
WHERE id = $1 LIMIT 1
`

func (q *Queries) GetWebhookDelivery(ctx context.Context, id uuid.UUID) (WebhookDelivery, error) {
	row := q.db.QueryRow(ctx, getWebhookDelivery, id)
	var i WebhookDelivery
	err := row.Scan(
		&i.ID,
		&i.TenantID,
		&i.RoomID,
		&i.RecordingID,
		&i.TranscriptID,
		&i.EventType,
		&i.WebhookUrl,
		&i.Payload,
		&i.Status,
		&i.Attempts,
		&i.MaxAttempts,
		&i.LastError,
		&i.NextRetryAt,
		&i.DeliveredAt,
		&i.CreatedAt,
	)
	return i, err
}

const markWebhookDelivered = `-- name: MarkWebhookDelivered :exec
UPDATE webhook_deliveries
SET status = 'delivered', delivered_at = NOW()
WHERE id = $1
`

func (q *Queries) MarkWebhookDelivered(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, markWebhookDelivered, id)
	return err
}

const markWebhookSending = `-- name: MarkWebhookSending :exec
UPDATE webhook_deliveries
SET status = 'sending'
WHERE id = $1
`

func (q *Queries) MarkWebhookSending(ctx context.Context, id uuid.UUID) error {
	_, err := q.db.Exec(ctx, markWebhookSending, id)
	return err
}

const updateWebhookDeliveryAttempt = `-- name: UpdateWebhookDeliveryAttempt :exec
UPDATE webhook_deliveries
SET
    status = $2,
    attempts = attempts + 1,
    last_error = $3,
    next_retry_at = $4
WHERE id = $1
`

type UpdateWebhookDeliveryAttemptParams struct {
	ID          uuid.UUID          `db:"id" json:"id"`
	Status      string             `db:"status" json:"status"`
	LastError   *string            `db:"last_error" json:"last_error"`
	NextRetryAt pgtype.Timestamptz `db:"next_retry_at" json:"next_retry_at"`
}

func (q *Queries) UpdateWebhookDeliveryAttempt(ctx context.Context, arg UpdateWebhookDeliveryAttemptParams) error {
	_, err := q.db.Exec(ctx, updateWebhookDeliveryAttempt,
		arg.ID,
		arg.Status,
		arg.LastError,
		arg.NextRetryAt,
	)
	return err
}
