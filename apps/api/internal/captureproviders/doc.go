// Package captureproviders resolves immutable Episode media-plane bindings to
// explicitly registered capture adapters. A provider is capture-capable only
// when its exact provider/application fingerprint is registered.
package captureproviders
