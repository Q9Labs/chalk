import { describe, expect, it } from "vitest";

import { ClassicCommandErrorAlert } from "./ClassicCommandErrorAlert";

describe("ClassicCommandErrorAlert", () => {
  it("exports the classic renderer", () => {
    expect(ClassicCommandErrorAlert).toBeDefined();
  });
});
